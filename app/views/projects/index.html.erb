<h1>Listing projects</h1>
<ul>
	<% @projects.each do |project| %>
		<li><%= link_to project.name, project %></li>
	<% end%>
</ul>

<!-- <table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Start at</th>
      <th>End at</th>
      <th>Expected progress</th>
      <th>Current progress</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
      <tr>
        <td><%= project.name %></td>
        <td><%= project.description %></td>
        <td><%= project.start_at %></td>
        <td><%= project.end_at %></td>
        <td><%= project.expected_progress %></td>
        <td><%= project.current_progress %></td>
        <td><%= link_to 'Show', project_path(project), method: :get, data: {confirm: "Show this project?"} %></td>
        <td><%= link_to 'Edit', edit_project_path(project), data: {confirm: "Edit this project?"} %></td>
        <td><%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->

<br>

<%= link_to 'New Project', new_project_path %>
